/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: false,
  // images: {
  //   loader: 'akamai',
  //   path: '/',
  // },
}

module.exports = nextConfig
